name: render pdf
on:
  push:

jobs:
  render:
    runs-on: ubuntu-latest 
    steps:
        - uses: quarto-dev/quarto-actions/setup@v2 
        - run: quarto render RSE_LAB.ipynb --to pdf --execute 


